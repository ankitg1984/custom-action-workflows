name: Custom

on: [push]

jobs:
  main:
    name: custom-desc
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2
    - uses: ankitg1984/custom-action@main
